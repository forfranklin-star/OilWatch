# 复制本文件为 .env 并按需修改；.env 已被 .gitignore 忽略，不会提交
# cp .env.example .env
# 全部信源（全球媒体/国内信源/机构/聚合/价格）均免费、无需任何 Token。

# ===== 时区（默认北京时间；日本改 Asia/Tokyo，并同步把 Actions cron 改为 '8 0 * * *'）=====
REPORT_TZ=Asia/Shanghai
